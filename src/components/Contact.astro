---
import Section from "./Section.astro";

const { email, socialLinks } = Astro.props;
---

<Section text="Let's Build Something Amazing Together" href="contact">
  <div class="max-w-4xl mx-auto text-center">
    <p class="mb-8 text-lg text-neutral leading-relaxed">
      I'm always excited to discuss new opportunities, challenging projects, and how Elixir can transform your business.
      Whether you need a consultant, full-time engineer, or technical advisor, let's talk.
    </p>
    
    <div class="grid md:grid-cols-2 gap-8 mb-12">
      <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-lg p-6 contact-card">
        <h3 class="text-xl font-semibold text-white mb-4">🚀 Ready to Scale?</h3>
        <p class="text-neutral mb-4">
          Need help with Elixir/Phoenix development, system architecture, or team mentoring?
        </p>
        <a 
          href={`mailto:${email}?subject=Let's discuss a project&body=Hi Fawad,%0D%0A%0D%0AI'd like to discuss...`}
          class="inline-block bg-primary text-white px-6 py-3 rounded-lg hover:bg-primary-light transition-all duration-300 hover:scale-105"
        >
          Start a Conversation
        </a>
      </div>
      
      <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-lg p-6 contact-card">
        <h3 class="text-xl font-semibold text-white mb-4">💼 Looking to Hire?</h3>
        <p class="text-neutral mb-4">
          Open to full-time, contract, and consulting opportunities with innovative teams.
        </p>
        <a 
          href={`mailto:${email}?subject=Opportunity Discussion&body=Hi Fawad,%0D%0A%0D%0AWe have an exciting opportunity...`}
          class="inline-block bg-primary text-white px-6 py-3 rounded-lg hover:bg-primary-light transition-all duration-300 hover:scale-105"
        >
          Discuss Opportunities
        </a>
      </div>
    </div>
    
    <div class="flex justify-center space-x-6">
      {socialLinks.map((link) => (
        <a 
          href={link.href}
          target="_blank"
          class="text-primary hover:text-primary-light transition-colors duration-300 text-lg font-medium"
        >
          {link.text}
        </a>
      ))}
    </div>
    
    <div class="mt-8 p-6 bg-primary/10 rounded-lg border border-primary/30">
      <p class="text-primary font-medium">
        🟢 Available for new projects starting immediately
      </p>
      <p class="text-neutral text-sm mt-2">
        Response time: Usually within 24 hours
      </p>
    </div>
  </div>
</Section>

<style>
  .contact-card {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.6s ease-out forwards;
  }
  
  .contact-card:nth-child(1) { animation-delay: 0.1s; }
  .contact-card:nth-child(2) { animation-delay: 0.3s; }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>